{
  "name": "translate-plugin",
  "version": "1.0.0",
  "description": "划词翻译助手 - Chrome extension for inline text translation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc --noEmit && node scripts/build.mjs && echo '✓ Build complete — load dist/ as unpacked extension'",
    "build": "tsc --noEmit && node scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
